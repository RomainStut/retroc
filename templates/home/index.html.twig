{% extends 'layout.html.twig' %}

{% block title %}Retroc{% endblock %}

{% block content %}

    <div class="container-fluid">
        <div class="row">

            {% for product in products %}

                <div class="card col-3" style="width: 18rem;">
                    <div class="card-body">
                        {% if product.image%}
                            <img class="card-img-top" src="{{ asset('uploads/images/' ~ product.image) }}" alt="Card image cap">
                        {% endif %}
                        <h5 class="card-title">{{ product.name }}</h5><hr>
                        <p class="card-text">{{ product.price }} €</p>
                        <a href="{{ path('product', {id: product.id })}}">Voir</a>
                    </div>
                </div>

            {% endfor %}

        </div>
    </div>

{#<div>#}
    {# equivalent de twig de la boucle foreach #}
    {#{% for blog in blogs%}#}

    {#<a href="{{ path('show',{id: blog.id})}}">les articles</a>#}
    {# à chaque tour de boucle on recupere qui ici est un objet#}
    {#pour acceder au titre, article.title #}
    {#{{ blog.title }}, <br> publie le  {{ blog.datepost|date('d/m/Y') }}#}
    {#<br>#}

    {#{{ blog.content}}#}
    {#<br>#}
    {#<img src="{{asset('upload/bo/images/')}}{{blog.image}}" alt="" />#}
{#</div>#}
{#{% endfor %}#}
{% endblock %}
